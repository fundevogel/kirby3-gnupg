label:
  de: PGP-Schlüssel
  en: PGP key
type: files
multiple: false
template: pubkey
query: site.files.filterBy('template', 'pubkey')
text: "ID: {{ file.key }}"
info: "{{ file.algorithm }} ({{ file.length }} bits)"
